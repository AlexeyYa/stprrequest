<Window x:Class="requestCreator.CorrectionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:requestCreator"
        mc:Ignorable="d"
        Title="CorrectionsWindow" Height="474.5" Width="800" WindowStartupLocation="CenterOwner" WindowStyle="None"
        >
    <Grid x:Name="CorGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Button x:Name="OkBtn" Margin="10,6" Content="OK" Click="OkBtn_Click" MaxHeight="30" MaxWidth="100"/>
        <Button x:Name="CancelBtn" Margin="10,6" Content="Отмена" Click="CancelBtn_Click" MaxHeight="30" MaxWidth="100"/>

    </Grid>
</Window>
